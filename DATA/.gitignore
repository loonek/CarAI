# Runtime outputs regenerated by the GA each run — not committed.
# track_data.json (the exported circuit dataset) IS kept, so the dataset
# is available to the supervisor at the shared link.
results/best_line.json
results/best_line.png
